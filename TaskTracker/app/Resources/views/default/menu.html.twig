<nav class="navbar navbar-default">
    <div class="container-fluid">
        <div class="navbar-header">
            <a class="navbar-brand" href="/">TaskTracker</a>
        </div>
        {% for project in projects %}
            <ul class="nav navbar-nav">
                <li class="{{ (selectedProjectId == project.getId) ? 'active' : '' }}">
                    <a href="/project/{{ project.getId }}">{{ project.getName }}</a>
                </li>
            </ul>
        {% endfor %}
        <ul class="nav navbar-nav navbar-right">
            <li><a>Привет, {{ app.user.getFullName() }}</a></li>
            <li><a href="/logout"><span class="glyphicon glyphicon-log-in"></span> Выйти</a></li>
        </ul>
    </div>
</nav>

